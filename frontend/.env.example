# API Configuration
VITE_API_URL=http://localhost:5000

# Vercel Deployment Flag (set to "true" when deploying to Vercel)
VITE_VERCEL=false

# Optional: If using a separate backend URL in production
VITE_PRODUCTION_API_URL=https://your-backend-project.vercel.app
